1、数据的全局Seq Id：
 .每个币对，或者交易量较高的币对，使用topic 上的一个分区，保证他们在分区中严格有序
 .订单生产者需要获取全局唯一的seq id,订单服务可以获取批量seq id 在本地节点，待有新订单分配，减少网路RT时间，同时在本地需要给批量Seq id设置过期时间
 .CEX撮合引擎接受订单，需要根据订单的seq id 判断该订单是否大于当前最大的seq id
   order_seq_id<=seq_id,跳过不处理
   order_seq_id=seq_id+1,正常处理
   order_seq_id>seq_id+1,放在本地队列，需要设置最长等待时间，时间一过，不在等待，处理队列里的数据

2.OrderBook数据结构：
  红黑树+双向链表，红黑树的节点存放链表，该链表存放同等价格档位的订单

3.业务流程，几种交易类型：
MARKET（市场单）
LIMIT（限价单）
POST_ONLY（只做 Maker）：
 Post-Only 订单确保你的订单只会以挂单（Maker）的身份进入订单簿，而不会立即与已有订单成交。如果提交时发现该订单会立即匹配（即变成 Taker），交易所会直接拒绝这笔订单，而不是执行它。
FOK（Fill or Kill，全部成交或立即取消）：
FOK 要求订单必须一次性全部成交，如果当前市场深度不足以一次性填满整个订单数量，则整笔订单立即取消，不会留下任何部分成交。  
IOC（Immediate or Cancel，立即成交剩余取消）：
IOC 订单提交后，能成交多少就立即成交多少，未能成交的剩余部分立即取消，不会作为挂单留在订单簿中。
ICEBERG（冰山订单）：
冰山订单用于将一笔大额订单拆分成多个小单，每次只在订单簿上展示一小部分（称为"可见量"），当这一小部分成交后，自动从隐藏的总量中补充下一批，直到全部成交或手动取消。

4.WAL增量与全量日志：
  1、撮合引擎发生交易后，产生对应的交易事件
  2、将事件写到RingBuffer，事件协程会先将事件写入一个增量缓冲区，然后写入全量快照中
  3、增量缓冲区会根据当前数据量及定时器（如500ms），发送到kafka然后写到增量日志中
  4、全量快照协程是定时（如5min）将数据订单簿快照持久化


5.ADL与RiskControl
 

