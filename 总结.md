### HIF高频交易：
1.  **Golang层面**：撮合引擎或其他需要大量CPU计算的逻辑，应将对应的协程绑定在固定的线程上，即对应的线程只有一个执行逻辑的协程。这可以减少协程上下文切换。

2.  **伪共享处理**：对于高频访问的数据，需要进行伪共享（False Sharing）的Padding填充。

3.  **GC优化**：
 *   减少堆内存分配。
 *   使用压舱石技术，减少GC频率。

4. **操作系统层面的优化**：
*  使用线程亲和性，绑定交易所进程到指定cpu，减少线程上下文导致的cpu L1 L2 缓存失效


### WAL日志持久化方案
1. **OrderBook全量快照持久化:**
* 双份缓冲+ringbuffer 环形缓冲
* 持久化使用FlatBuffers序列化+bufio
2. **OrderBook增量快照持久化:** 
* 持久化使用FlatBuffers序列化+bufio



### 流程
1. 用户网关，对订单的参数进行校验，账户余额是否充足，以及定额冻结
2. 发送到定序器，对每个币种进行定序
3. 数据发送到kafka，数据写入成功返回给前端
4. 撮合引擎消费数据，撮合结果发送到下游服务
5. 行情推送

### 定序器作用
* 定序器是从业务上给订单一个序号，不依赖中间件例如kafka
* 好处是：
1. **上游如果因为某些原因重复发送，撮合引擎可以根据seq id 进行幂等去重操作**
2. **假如kakfa 增加分区或者其他导致分区变化操作，依赖kafka 分区保证顺序撮合变得不可靠，只能通过seq id 去保证**