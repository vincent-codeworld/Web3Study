1 **获取每个区块，获取区块中的每笔交易并判断是否数据业务需要的数据：**
* 判断交易是不是普通ETH转账还是合约转账,如果是普通转账，要判断发送地址或者接受地址是不是我们自己管理的账号
* 如果是合约，要看是合约转ETH还是普通调用合约方法，具体看看Value是否大于0，to的地址是不是自己管理的合约地址
* 如果是普通调用合约方法，就要判断to对应合约地址属于哪一个地址
* 获取交易的同时获取收据（使用eth_getBlockReceipts），交易的下标跟收据下标一样，需要通过收据来判断交易的状态，是否交易成功
* 处理收据的同时需要解析事件 

2 **使用websocket监听特定事件，实时性强但是容易受网络影响，另外数据的可靠性比较差，适合一些实时要求高的场景**

3 **NFT监听的事件**
.Transfer事件 (ERC-721):
 Transfer(address indexed from, address indexed to, uint256 indexed tokenId)
 MetadataUpdate(uint256 _tokenId)：单体更新 [元数据更新事件 (EIP-4906)]
 BatchMetadataUpdate(uint256 _fromTokenId, uint256 _toTokenId)：批量更新（如统一开盲盒）[元数据更新事件 (EIP-4906)]
 Approval(address owner, address approved, uint256 tokenId)：单体授权
 ApprovalForAll(address owner, address operator, bool approved)：全部授权（最常见，用户在 Opensea 挂单前都会触发这个）



ETH transfer:
     From(EOA)->(ETH)Contract,  Value>0
     From(EOA)->Contract A (ETH)-> （EOA）或者 Contract B ,Value=0,需要监听日志


1、交易类型区分
2、交易、事件的唯一标识
普通转账：chain_id, tx_hash
合约事件：chain_id, tx_hash, log_index
回执：chain_id, tx_hash
区块：chain_id, block_hash



一个区块含有多笔交易，每笔交易都有一个回执，记录着：status、gasUsed、logsBloom、以及 logs（事件日志数组） 等。
一个回执有多个事件日志


事件中Topic：
0:事件的签名
1:第一个indexed参数
2:第二个indexed参数
3:第二个indexed参数

最多三个indexed 参数，非 indexed 参数在 Data 中，匿名事件没有签名topic，最多可有4个索引参数
可以通过indexed参数快速检索数据，类似数据库中的索引


区块
｜
｜
交易
｜
｜
回执
｜
｜
事件日志

真实生产，获取哪些数据，怎么存






3  **
扫链：
interview内容
Resume ：
1、NFT项目
元数据服务 (Metadata API)
链上数据同步与索引 (Indexing & Syncing)
NFT撮合交易，线下操作
2、AI客服（未上线）
3.了解AMM
rootdata挂靠项目
CEX：
interview内容
rootdata挂靠项目
golang tech statck：
1、PG、redis、kafka、mysql
2、golang 基础知识
3、观测性
区块链基础知识：
1、ETH


目前优先级：
1、修改Resume
2、复习



NFT项目
项目简介：
Element是一个多链 NFT 聚合交易平台，其核心业务由"聚合市场"与"自有订单簿市场"两部分构成。我于 2022 年底加入团队，主要负责"自有订单簿市场"的架构优化、性能重构与新功能迭代。该自有市场采用"链下签名订单簿 + 链上批量结算"的混合架构模式，
作为 Element 聚合生态的重要组成部分，为专业交易者提供低成本、高效率的交易体验。

后端技术栈：Go、PostgreSQL、Mysql、Redis、MongoDB、Kubernetes、Docker


主要职责：
1、设计并实现高并发核心业务 API。通过多级缓存策略缓存热点数据、优化复杂 SQL 查询，使核心接口 P99 响应时间降低至 50ms 以内，单节点可支撑 上万 QPS。
2、构建高可用的链上结算与事件同步服务:
.链上事件监听与数据同步：构建并持续优化了高可用的链上事件监听服务，设计并实现了针对链重组（Reorg）的自动回滚与数据修正机制，确保 关键事件毫秒级同步至后端数据库，保证了数据最终一致性。通过优化事件处理流程，数据与链上状态的延迟始终保持在 3 秒以内。
.多链事件同步架构：设计了统一的多链事件监听框架，支持以太坊、Polygon、BNB Chain 等不同 EVM 链的并发监听，通过连接池管理和重试机制，确保了多链环境下的高可用性。

项目技术难点及优化：
1、Goroutine泄露排查及解决
2、使用池化技术减轻GC压力
3、使用压舱石技术，减少GC频率
4、接口优化,将API响应时间从初期的500毫秒优化到150毫秒




流程：
卖家挂售：
钱包签名 → POST /api/v1/orders (side=0) → 订单簿存储

买家浏览：
GET /api/v1/listings?nft_contract=0x...&token_id=6529
→ 前端展示价格列表

买家购买（Buy Now）：
GET /api/v1/orders/{hash}  ← 取出卖家的完整签名订单
→ 前端用卖家签名 + 买家自己的参数，调用合约 fulfillOrder()
→ 链上原子交换完成
→ 后端监听链上事件 → MarkFilled(orderHash)

查询：
1、Nft级别：价格区间、支付代币、状态、Traits（属性）、稀有度排名、Token ID
2、Collection级别

====================================================================================
项目名称：企业级智能客服与知识库系统 (AI & RAG) - 核心研发阶段
1. 项目简介
   本项目旨在利用大语言模型（LLM）与 RAG（检索增强生成）技术，为企业构建专属的私有化问答系统，以降低人工客服成本。作为核心后端开发，主导了系统 API 架构的从零到一搭建以及知识库检索链路的开发。项目侧重于系统的可扩展性、业务逻辑的准确性以及高质量的代码工程实践，目前已完成核心功能开发与内部验证。

2. 技术栈
   Golang, enio (或常规 Web 框架), Redis, Milvus (向量数据库), SSE (流式传输), LLM API (大模型接入)

3. 主要职责与技术落地（突出工程质量与业务准确度）
   规范化 API 架构与工程实践：
   主导后端业务 API 的设计与开发，严格遵循 RESTful 规范。基于 Golang 构建了高内聚、低耦合的系统分层架构；
   统一定义了全局错误码与异常拦截机制，并开发了鉴权、日志追踪（TraceID）等基础中间件，极大提升了 API 的安全性、可观测性与后续的可维护性。
   RAG 检索链路与准确度优化：
   负责企业私有文档的解析与向量化（Embedding）处理。针对长文本业务场景，设计了合理的文本分块（Chunking）策略；
   结合 Milvus 向量数据库实现高效检索，并在内部测试中引入“向量相似度 + 关键词”的混合召回思路，不断调优 Prompt 模板，有效减少了 AI 的“幻觉”，提升了专业问答的准确率。
   会话管理与 Token 成本控制：
   针对多轮对话场景，设计了基于 Redis 的会话状态（Session）管理机制；
   实现了基于 Token 长度的滑动窗口截断算法，在保证大模型能够精准理解历史上下文的同时，有效控制了单次 API 调用的 Token 消耗，降低了企业运营成本。
   外部依赖治理与体验优化：
   针对第三方大模型 API 响应慢、偶发超时的痛点，在 API 接入层实现了完善的超时控制与指数退避重试机制，增强了系统的健壮性；
   采用 SSE (Server-Sent Events) 协议开发流式输出接口，实现 AI 回答的“打字机”效果，大幅降低了首字响应时间（TTFB），优化了前端用户的交互体验。








CEX交易所
技术栈： Golang, gRPC, Kafka, Redis, MySQL, K8s，Docker

项目描述：
作为 CEX 交易系统的核心大脑，负责接收用户委托单并按“价格优先、时间优先”原则进行极速撮合，生成成交记录并推送至下游清算与行情系统。项目需满足极高的吞吐量与微秒级延迟要求。


主要职责：

1.基于 Golang 设计并实现了纯内存订单簿（Order Book），采用 红黑树 (Red-Black Tree)  维护价格档位，使用 双向链表 维护同价位订单，实现 接近O(1)时间复杂度。
实现严格的价格时间优先（Price-Time Priority）撮合逻辑，支持市价单、限价单、FOK、IOC 等多种复杂委托类型。
2.高并发与性能优化

无锁化设计： 针对热点交易对（Symbol）采用单 Goroutine 串行处理核心撮合逻辑，避免锁竞争开销；通过 RingBuffer 接收上游网关订单，实现无锁化的高效流转。

3.异步落盘与快照
采用 WAL (Write-Ahead Logging) 机制，将撮合指令异步顺序写入磁盘，结合定时内存快照 (Snapshot)，确保引擎宕机重启后能在 3 秒 内精准恢复订单簿状态，保证数据零丢失。
解耦与异步化： 撮合成功后，通过 Kafka 将成交明细（Trades）异步广播至清算中心、K线服务和 WebSocket 推送网关，实现核心撮合与下游重业务的物理隔离。

项目优化：
1.针对高频交易场景下的内存抖动问题，大量应用 sync.Pool 复用订单对象和撮合结果对象；
2.优化逃逸分析，减少堆内存分配，将核心撮合链路的 GC 停顿（STW）控制在 x 毫秒 以内。
3.调整GOGC、GOMEMLIMIT 参数，减少GC频率，并使用CPU亲和性，保证撮合线程跟GC不在同个线程